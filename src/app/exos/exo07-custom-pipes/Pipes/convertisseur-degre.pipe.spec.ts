import { ConvertisseurDegrePipe } from './convertisseur-degre.pipe';

describe('ConvertisseurDegrePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertisseurDegrePipe();
    expect(pipe).toBeTruthy();
  });
});
